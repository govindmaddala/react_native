import { StyleSheet } from "react-native";

export default externalStyling = StyleSheet.create({
    externalTextStyle: {
        color: "blue",
        fontStyle: "italic",
        fontSize: 20,
        textDecorationLine: "underline"
    }
})